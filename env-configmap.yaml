kind: ConfigMap
apiVersion: v1
metadata:
  name: mono-config
data:
  # Configuration values can be set as key-value properties
  POSTGRES_HOST: keluemonodb.cmzjhsq2i98a.us-east-2.rds.amazonaws.com
  POSTGRES_DB: monodb
  AWS_BUCKET: arn:aws:s3:::kelue-mono-bucket
  AWS_REGION: us-east-2
  AWS_PROFILE: default
  JWT_SECRET: testing
  URL: http://localhost:8100
